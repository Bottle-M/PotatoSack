# PotatoSack 3.0.0 设计

## 1. 增量备份体积大幅优化

现有增量备份机制有明显问题: 对于 Minecraft 存档这种 MCAnvil (`.mca`) 体积占比很大的区域文件，就算玩家只动了一个方块，甚至没有动方块，仅仅是在区块附近导致区块的 `InhabitedTime` 等属性发生变动，可能也要备份这一整个区域文件，其实还是挺浪费空间的。

针对这一点，在增量备份比较变动时，我们需要特殊处理 `.mca` 文件的变动比较，相对应的，备份记录文件 (`_备份路径标识.json`) 也需要有较大变化（需要记录更细节的 `.mca` 中区块内容的差别，因此存储结构也需要变得更紧凑）。

### 1.1. 备份记录文件格式

涉及 `.mca` 时需要存储每个区块的状态信息。存储和 NBT 一样，为了节省存储空间，需要设计更为紧凑的存储格式。

```
[ uint32 存储格式版本号 ] [ uint64 存储最后修改时间戳 ] [ uint64 文件数量 ] [ 文件最后修改时间表 ] [ 文件哈希表 ] [ 文件路径字符串长度表 ] [ 紧凑的文件路径字符串表 ] [ mca 数据部分 ]
```

#### 1.1.1. 主要字段说明

* **存储格式版本号** (`uint32`): 记录当前备份记录文件的格式版本，确保以后紧凑存储结构发生更改仍能做兼容。
* **最后修改时间戳** (`uint64`): 记录当前备份记录文件的最后修改时间戳。
* **文件数量** (`uint64`): 当前备份记录文件所存储的文件记录数。

**注 1**：文件路径字符串已经隐含了文件类型，可以用后缀名判断（Minecraft 本体也是靠后缀名来找到区域文件的），不需要额外的表来记录文件类型。  
**注 2**：有很多紧凑的表是为了压缩率着想，这样压缩算法更容易发现重复模式。

* **文件最后修改时间表** (`uint64[]`): 每条记录对应文件的最后修改时间拼接得到 (长度为文件数量)。
* **文件哈希表** (`uint64[]`): 每条记录对应文件的 XXH3_64 哈希拼接得到 (长度为文件数量)。
* **文件路径字符串长度表** (`uint16[]`): 每条记录对应的相对路径占用的字节数 (长度为文件数量)。
* **紧凑的文件路径字符串表** (`byte[]`): 根据**文件路径字符串长度表**存储指定长度的文件相对路径字符串。

根据文件路径字符串，我们能筛选出 `.mca` 文件，设有 `mcaLen` 个文件。然后在 `mca 数据部分` 中存储每个 `.mca` 文件的区块最后变更时间戳（即区域文件格式文件头的 4 KiB 区块时间戳），按照 `.mca` 出现的顺序存储：  

* 每个 `.mca` 文件一个 `uint64[1024]` 的数组，存储每个区块的最后变更时间戳。

<!-- 变化区块相比全部区块数量可能是很稀疏的 -->

